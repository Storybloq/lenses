{
  "blockedBy": [
    "T-022"
  ],
  "claimedBySession": "9a542e21-4fe8-4635-8b19-d0b5029bfc5d",
  "completedDate": "2026-04-24",
  "createdDate": "2026-04-23",
  "description": "**Problem.** Process-local state is fragile by design:\n- `sessions` Map at `review-state.ts:96` has no eviction → slow memory leak in long-running MCP servers.\n- Server restart between hop-1 and hop-2 destroys the `reviewId` (in-memory only).\n- T-014's disk cache persists only *completed rounds* — not in-flight state.\n\n**Non-goal: SQLite.** `cache/session.ts` already has the right primitives (atomic tmp+rename, mode 0o600, schema-versioned, TTL sweep). Extending that is ~10x cheaper than `better-sqlite3`.\n\n---\n\n**Design — DECIDED: disk is source of truth; Map becomes a bounded LRU read-cache.**\nNot \"add eviction to the Map\" — that keeps the Map authoritative and the disk as a side-channel. The correct design once disk records exist is:\n1. `lens_review_complete` reads records from disk by `(reviewId, lensId, attempt)`.\n2. Recent reads populate a bounded LRU Map (default 100 entries).\n3. Map miss → disk read → Map populate. Cheap because disk reads are cheap.\n4. Map eviction on size cap is fine; disk record is still there. Eviction is not data loss.\n\nThis makes restart-between-hops work for free — the Map is ephemeral, disk is durable.\n\n**Path scheme — FIXED.** Use `tmpdir()/lenses-in-flight/<reviewId>/<lensId>.<attempt>.json`. Match the existing `tmpdir()/lenses-sessions/` convention from `cache/session.ts`. Do NOT use a project-local `.lenses/in-flight/` — mixing tmpdir + project-local state confuses users and muddies the cleanup contract.\n\n**Record shape:**\n```\n{\n  taskId: string,                  // sha256(reviewId + \":\" + lensId + \":\" + attempt)\n  reviewId: string,\n  lensId: LensId,\n  attempt: number,                 // 1-indexed; T-022 max_attempts default is 2\n  status: \"pending\" | \"in_flight\" | \"completed\" | \"failed\" | \"expired\",\n  promptHash: string,\n  chunkIndex: number | null,       // nullable day 1 for T-025 chunking; avoids a second migration\n  startedAt: string,               // ISO 8601\n  completedAt: string | null,\n  expiresAt: string,               // ISO 8601; ownership: T-022 server-side at hop-1 from T-023's lensTimeout\n  errorCode: LensErrorCode | null  // typed enum — see below\n}\n```\n\n**Typed error enum — DEFINED.** Codex's critique (#6) was right: control-plane codes matter more in this architecture than model-flavored codes, because the server doesn't call LLMs.\n\n```\ntype LensErrorCode =\n  | \"PARSE_FAILURE\"       // finding-level .strict() failed; lens surfaced in parseErrors[]\n  | \"DUPLICATE_COMPLETE\"  // second lens_review_complete for same (reviewId, lensId, attempt)\n  | \"REVIEW_EXPIRED\"      // resubmission past expiresAt\n  | \"REVIEW_CANCELLED\"    // caller cancelled round\n  | \"PARTIAL_RESULTS\"     // some lenses timed out; verdict derived from survivors\n  | \"MERGE_CONFLICT\"      // two completions with incompatible state\n  | \"CONFIG_MISMATCH\"     // mergerConfig changed mid-round\n  | \"AGENT_TIMEOUT\"       // lens exceeded expiresAt without completing\n  | \"UNKNOWN_ERROR\";\n```\nEach code paired with a user-facing actionable message (follow codex-bridge's `src/utils/errors.ts` pattern).\n\n**Files touched.**\n- `src/cache/in-flight.ts` — NEW. Mirrors `src/cache/session.ts` primitives (atomic tmp+rename, 0o600, schema versioning, TTL sweep).\n- `src/tools/start.ts` — write `status: \"pending\"` record per activated lens on hop-1.\n- `src/tools/complete.ts` — read records on hop-2; idempotent on `(reviewId, lensId, attempt)`; update `status`/`completedAt`/`errorCode`. Preserve the outside-outer-try/catch cache-isolation at line 400-401.\n- `src/tools/get-prompt.ts` (NEW in T-022) — reads `promptHash` from disk records.\n- `src/review-state.ts` — Map becomes bounded LRU read-cache; eviction is no-op on data (disk has it).\n- `src/schema/error-code.ts` — NEW. Exports `LensErrorCode` union.\n- Tests: restart-mid-review recovery; `(reviewId, lensId, attempt)` idempotence; typed-enum exhaustiveness (bidirectional `Record<LensErrorCode, true>` map, same pattern as `LensId` in T-021's public-api.test.ts).\n\n**Preserve RULES.md §4.** Structural discipline: persistence failure must never flip `isError: true`. Calls run outside the outer try/catch in `complete.ts` (same pattern as `persistRoundBestEffort` at line 400-401).\n\n**Acceptance.**\n- `reviewId` generated by `lens_review_start` is recoverable by `lens_review_complete` after an MCP-server restart. Test uses an in-process server restart harness.\n- `sessions` Map is a bounded LRU read-cache with disk as source of truth. Test asserts: populate cache to cap, evict LRU entry, subsequent read finds it on disk.\n- Every record has `chunkIndex?: number | null` — pinned by Zod schema.\n- `LensErrorCode` enum is exhaustively covered (compile-time and runtime).\n- RULES.md §4 structurally preserved — persistence failure never flips `isError: true`. Test: mock `fs.writeFile` to throw; assert review still succeeds.\n\n**Blocked by T-022** — T-022 decides `expiresAt` ownership and the `(reviewId, lensId, attempt)` idempotence key shape that this ticket persists.",
  "id": "T-024",
  "order": 80,
  "phase": "integration",
  "status": "complete",
  "title": "Disk persistence for in-flight review state (extend cache/session.ts JSON pattern)",
  "type": "task"
}
